<header>
  <h1>{{page.title}}</h1>
</header>

<section>
   <main>
  <span>oi</span>

    {% assign products = collections.all.products %}
    
   {%for produtos in products %}
    <span>{{product}}</span>
    {%endfor%}
   </main>
</section>











{% schema %}
{
    "name": "lotion-lp",
    "settings": [
        
        ],

    "presets": [{
        "name": "lotion-lp"
    }
        
    ]
}
{% endschema %}