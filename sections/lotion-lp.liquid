{{'lotion-lp.css' | asset_url | stylesheet_tag}}


<header class="cabecalho"><h1>{{page.title}}</h1></header>


<body>
    <section>

        <main>
         <p>{{page.content}}</p>
         <br>

        <div class="produto-principal">
                {% assign product = section.settings.locao-principal%}
                <img src="{{product.featured_image | image_url: width: 30 }}"alt="{{product.featured_image.alt}}"/>
                <h2>{{product.title}}</h2>
                <p>{{product.price | money}}</p>
                <a href="{{product.url}}">{{product.title}}</a>
        </div>

        

        <section class="info-colecao">
        {% assign collection = section.settings.colecao%}
        <h2>{{collection.title}}</h2>
        <p>{{collection.description}}</p>

        </section>



        </main>


        {% for product in collection.products%}

            <div class="produtos-colecao">
        
            <h3>{{product.title}} </h3>
            <p>{{product.price | money}}</p>
            <img src="{{product.featured_image |  image_url: width: 20}}" alt="{{product.featured_image.alt}}" />
            </div>
        
        {% endfor %}
        
    </section>
</body>




{% schema %}
{
    "name": "lotion-lp",
    "settings": [
            {
                "type" :"collection",
                "id": "colecao",
                "label": "Coleção"
            },

            {
                "type": "product",
                "id": "locao-principal",
                "label": "Loção Principal"
            }
        ],

    "presets": [{
        "name": "lotion-lp"
    }
    
        
    ]
}
{% endschema %}